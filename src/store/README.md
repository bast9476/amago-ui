# Store

Global state management (Redux/Zustand/Context) for app-wide state that needs to be shared across modules.

Module-specific state should be in their respective `modules/{module}/store/` folders.

